# Unsupervised Link Prediction AUC
A Python implementation for calculating AUC as an evaluation metric for unsupervised link prediction problem.

$$\mathrm{AUC} = \frac{n^{\prime} + 0.5n^{\prime\prime}}{n}$$
